# Vednix AI — backend runtime dependencies (Python 3.11+)
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
pydantic>=2.7
pydantic-settings>=2.3
psutil>=5.9
# Phase 4 — document readers (pure-Python, fully offline)
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
python-multipart>=0.0.9
# Phase 5 — research agent (pure-Python orchestration)
langgraph>=0.2
# Phase 5 — optional infra (imported lazily; safe to install always)
redis>=5.0
