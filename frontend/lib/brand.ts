/**
 * Single source of truth for product + creator branding.
 * BRAND_NAME is always the primary identity; the creator signature stays
 * subtle and secondary — everywhere it appears, it renders through
 * components/brand/Signature.tsx so typography never drifts.
 */
export const BRAND_NAME = "Vednix AI";
export const CREATOR_NAME = "Abhinav Singh";
export const CREATOR_SIGNATURE = `Made by ${CREATOR_NAME}`;
